# frozen_string_literal: true

RSpec.describe OmniauthLoginUnico do
  it "has a version number" do
    expect(OmniauthLoginUnico::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
